@import "./reset"

$clos=12 //定义栅格数

.container
    // width auto
    //默认 mobile first 优先 
    margin-right auto
    margin-left  auto
    padding-left 15px
    padding-right 15px
.row
    display flex
    flex-wrap nowrap 
    margin-left -15px
    margin-right -15px
//12份
for $v in 1..$cols
    .col-{$v}
        width(100/12 * $v)%
